import type { AssessmentQuestion } from "@/types/assessment";
export const workflowQuestions: AssessmentQuestion[] = [{ id: "workflow_inventory", module: "workflows", field: "workflows", title: "Inventory 8–10 significant recurring workflows", help: "One per line: Workflow | function | owner | monthly volume | minutes each | bottleneck | systems. Include operations, customers, people, finance, marketing, and management work.", priority: 95, isComplete: (a) => a.workflows.length >= 10 && a.workflows.every((w) => Boolean(w.workflow_name && w.function_name && w.bottlenecks.length)) }];
